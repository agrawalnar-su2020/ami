{
    "variables": {
        "aws_access_key": "",
        "aws_secret_key": "",
        "aws_region": "",
        "subnet_id": "",
        "source_ami": "ami-068663a3c619dd892",
        "ssh_username":"ubuntu",
        "ami_users": "213094423378"
    },
    "builders": [
        {
            "type": "amazon-ebs",
            "ami_users": "{{ user `ami_users` }}",
            "access_key": "{{user `aws_access_key`}}",
            "secret_key": "{{user `aws_secret_key`}}",
            "region": "{{user `aws_region`}}",
            "instance_type": "t2.micro",
            "subnet_id": "{{user `subnet_id`}}",
            "source_ami": "{{user `source_ami`}}",
            "ssh_username": "{{user `ssh_username`}}",
            "ami_name": "csye6225_a4{{timestamp}}",
            "ami_description": "Ubuntu AMI for CSYE 6225",
            "launch_block_device_mappings": [
                {
                    "device_name": "/dev/sda1",
                    "volume_size": 8,
                    "volume_type": "gp2",
                    "delete_on_termination": true
                }
            ]
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "environment_vars": [
                "FOO=foo"
            ],
            "inline": [
            	"sleep 30",
                "sudo apt-get update",
                "sudo apt-get install default-jdk -y",
                "sudo apt-get update",
                "sudo apt-get install tomcat9 tomcat9-docs tomcat9-examples tomcat9-admin apache2 -y"
                
            ]
        }
    ]
}